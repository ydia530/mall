<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yuan.mall.mapper.UmsRoleMapper">

    <select id="getResourcesById" resultType="com.yuan.mall.entity.ums.UmsResource">
        select b.id, b.name
        from ums_role_resource_relation a join  ums_resource b on a.resource_id = b.id
        where a.role_id = ${roleId}
    </select>

</mapper>